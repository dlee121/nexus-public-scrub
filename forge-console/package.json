{
  "name": "forge-console",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "start": "bun run src/server.ts",
    "smoke": "bun run src/smoke.ts",
    "smoke-stream": "bun run src/smoke-stream.ts",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "@temporalio/client": "^1.11.0",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  }
}
